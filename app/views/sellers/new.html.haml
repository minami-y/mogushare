:javascript
  $(document).on('turbolinks:load', function() {
    $('.individual_info, .bank-info, .submit-button, .add-button').hide();
    $('.next-button').on('click', function(){
      $('.basic_info').hide();
      $('.individual_info, .submit-button').show();
    });
  });

.board.large.color-none
  %h1.title 投稿にはプロフィールが必須です(簡単2STEP)
  .board.middle.color-none
    = render 'form', seller: @seller
