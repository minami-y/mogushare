%h1 Login

= form_for(:session, url: login_path) do |f|

  = f.label :email
  = f.email_field :email

  = f.label :password
  = f.password_field :password

  = f.submit "ログイン"

= link_to "新規登録はこちら", signup_path
